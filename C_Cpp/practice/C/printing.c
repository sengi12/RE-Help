#include <stdio.h>
#include <stdlib.h>

int main()
{
    // format specifier % (d, s, c)
    // d = integer
    // f = decimal
    // c = character
    // s = string
    int favNum = 12;
    printf("My favorite %s is %d\n", "number", favNum);

    return 0;
}